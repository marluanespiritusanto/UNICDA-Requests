const { RoleServiceMock } = require("../mocks");

describe("Role Service", () => {
  beforeEach(async () => {});

  it("should find a role", () => {
    const role = { id: 1, name: "GENERIC", status: "ACTIVE" };
  });
});
